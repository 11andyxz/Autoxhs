#!/usr/bin/env node
import { spawnSync } from "node:child_process";
import ffprobeStatic from "ffprobe-static";

const result = spawnSync(ffprobeStatic.path, process.argv.slice(2), {
  stdio: "inherit",
});

process.exit(result.status ?? 1);
