#!/usr/bin/env node
import { spawnSync } from "node:child_process";
import ffmpegPath from "ffmpeg-static";

const result = spawnSync(ffmpegPath, process.argv.slice(2), {
  stdio: "inherit",
});

process.exit(result.status ?? 1);
